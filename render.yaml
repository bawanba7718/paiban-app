services:
  - type: web
    name: paiban-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run paiban.py --server.port $PORT --server.address 0.0.0.0
